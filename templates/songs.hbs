{{#each songs}}
	<div>
		<h3 class="songnames">{{title}}</h3>
		<h4>{{artist}}</h4>
		<h4>{{album}}</h4>
		<div class="row">
			<div class="col-sm-12">
				<a href="#" id="delete#{{ @key }}">Delete</a>
			</div>
		</div>
	</div>
{{/each}}